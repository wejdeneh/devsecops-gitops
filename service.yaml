apiVersion: v1
kind: Service

metadata:
  name: devsecops-demo-service

spec:
  type: NodePort

  selector:
    app: devsecops-demo

  ports:
  - name: http
    port: 80
    targetPort: 5000
    nodePort: 30090
